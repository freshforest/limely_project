import { Box, Typography } from "@mui/material";

const ReviewsIo = () => {
  return (
    <Box sx={{ background: "#ffffff", p: 5, my: 4, textAlign: "center" }}>
      <Typography variant="h2">Reviews.io widget</Typography>
    </Box>
  );
};

export default ReviewsIo;
